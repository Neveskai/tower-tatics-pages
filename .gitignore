node_modules/
.env

# Generated by scripts/build-blog.mjs — CI (blog.yml) rebuilds these fresh on
# every deploy, so they don't need to live in git history. (blog/social/**,
# generated by generate-social.mjs, is intentionally NOT ignored — that
# content is meant to be committed for manual review.)
/blog/index.html
/blog/*/index.html
